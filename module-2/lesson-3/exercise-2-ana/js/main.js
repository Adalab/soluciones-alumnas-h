'use strict';

const name = 'Maria';


if (name === 'Maria' || name === 'Ana') {
    console.log(`Bienvenida, ${name}`);
} else {
    console.log('Lo siento pero el usuario que has introducido no está registrado');
}