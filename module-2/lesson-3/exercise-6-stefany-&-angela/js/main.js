'use strict';

let avocados;
const avocadoPrice = 1.5;
const money = 0;

const numberOfAvocados = (money >= avocadoPrice) ? avocados = money / avocadoPrice : avocados = 0;

console.log(numberOfAvocados);